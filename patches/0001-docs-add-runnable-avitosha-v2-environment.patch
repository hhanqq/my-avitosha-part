From f7289e523da7383e87ee22a96fe02b62451a60da Mon Sep 17 00:00:00 2001
From: sergey <152873255+hhanqq@users.noreply.github.com>
Date: Wed, 5 Aug 2026 10:16:21 +0300
Subject: [PATCH] docs: add runnable avitosha v2 environment

---
 app/backend/api/openapi.yaml        | 880 +++++++++++++++-------------
 app/backend/api/openapi_test.go     |  24 +-
 app/backend/scripts/init-test-db.sh |   6 +
 app/backend/scripts/smoke-game.sh   |  47 ++
 app/backend/scripts/smoke-pet.ps1   | 173 ------
 5 files changed, 556 insertions(+), 574 deletions(-)
 create mode 100755 app/backend/scripts/init-test-db.sh
 create mode 100755 app/backend/scripts/smoke-game.sh
 delete mode 100644 app/backend/scripts/smoke-pet.ps1

diff --git a/app/backend/api/openapi.yaml b/app/backend/api/openapi.yaml
index 7206e52..484bec9 100644
--- a/app/backend/api/openapi.yaml
+++ b/app/backend/api/openapi.yaml
@@ -1,191 +1,190 @@
 openapi: 3.0.3
 info:
   title: Avitosha API
-  version: 1.0.0
-  description: |
-    Authentication and virtual pet contract for the Avitosha MVP backend.
+  version: 2.0.0
+  description: Gamification layer that turns useful Avito actions into Avitosha progress.
 servers:
   - url: http://localhost:8080
-    description: Local development
+
 tags:
+  - name: health
   - name: auth
-    description: Registration, login, session refresh, logout
-  - name: user
-    description: Current authenticated user
-  - name: pet
-    description: Avitosha state, care items, growth, and daily summary
+  - name: game
+  - name: realtime
+
 paths:
+  /healthz:
+    get:
+      tags: [health]
+      operationId: healthz
+      responses:
+        '200':
+          description: Process is alive
+  /health/live:
+    get:
+      tags: [health]
+      operationId: healthLive
+      responses:
+        '200':
+          description: Process is alive
+  /health/ready:
+    get:
+      tags: [health]
+      operationId: healthReady
+      responses:
+        '200':
+          description: PostgreSQL is reachable
+        '503':
+          $ref: '#/components/responses/InternalError'
+
   /api/auth/register:
     post:
       tags: [auth]
-      summary: Register a new user
       operationId: register
       requestBody:
         required: true
         content:
           application/json:
             schema:
-              $ref: '#/components/schemas/AuthCredentialsRequest'
-            examples:
-              default:
-                value:
-                  email: user@example.com
-                  password: password123
+              $ref: '#/components/schemas/AuthCredentials'
       responses:
         '201':
-          description: User registered successfully
-          headers:
-            Set-Cookie:
-              $ref: '#/components/headers/RefreshTokenSetCookie'
+          description: User and session created
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/AuthResponse'
-              examples:
-                default:
-                  value:
-                    access_token: <JWT_ACCESS_TOKEN>
-                    user:
-                      id: 8f0ed065-aefa-4f56-87d0-e2ef2ef43f0d
-                      email: user@example.com
         '400':
           $ref: '#/components/responses/InvalidRequest'
         '409':
-          $ref: '#/components/responses/EmailAlreadyExists'
-        '500':
-          $ref: '#/components/responses/InternalError'
+          $ref: '#/components/responses/Conflict'
   /api/auth/login:
     post:
       tags: [auth]
-      summary: Log in with email and password
       operationId: login
       requestBody:
         required: true
         content:
           application/json:
             schema:
-              $ref: '#/components/schemas/AuthCredentialsRequest'
-            examples:
-              default:
-                value:
-                  email: user@example.com
-                  password: password123
+              $ref: '#/components/schemas/AuthCredentials'
       responses:
         '200':
-          description: Login successful
-          headers:
-            Set-Cookie:
-              $ref: '#/components/headers/RefreshTokenSetCookie'
+          description: Session created
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/AuthResponse'
-              examples:
-                default:
-                  value:
-                    access_token: <JWT_ACCESS_TOKEN>
-                    user:
-                      id: 8f0ed065-aefa-4f56-87d0-e2ef2ef43f0d
-                      email: user@example.com
         '400':
           $ref: '#/components/responses/InvalidRequest'
         '401':
-          $ref: '#/components/responses/InvalidCredentials'
-        '500':
-          $ref: '#/components/responses/InternalError'
+          $ref: '#/components/responses/Unauthorized'
   /api/auth/refresh:
     post:
       tags: [auth]
-      summary: Rotate refresh token and issue a new access token
-      operationId: refreshSession
-      security:
-        - refreshTokenCookie: []
+      operationId: refresh
       responses:
         '200':
-          description: Token pair rotated successfully
-          headers:
-            Set-Cookie:
-              $ref: '#/components/headers/RefreshTokenSetCookie'
+          description: Access and refresh tokens rotated
+          content:
+            application/json:
+              schema:
+                type: object
+                required: [access_token]
+                properties:
+                  access_token:
+                    type: string
+        '401':
+          description: Refresh cookie is missing or the session expired
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/RefreshResponse'
+                $ref: '#/components/schemas/ErrorResponse'
               examples:
-                default:
+                missing_refresh_cookie:
                   value:
-                    access_token: <JWT_ACCESS_TOKEN>
-        '401':
-          $ref: '#/components/responses/RefreshUnauthorized'
-        '500':
-          $ref: '#/components/responses/InternalError'
+                    error:
+                      code: unauthorized
+                      message: Authentication is required
+                expired_session:
+                  value:
+                    error:
+                      code: session_expired
+                      message: Session has expired or is no longer valid
   /api/auth/logout:
     post:
       tags: [auth]
-      summary: Revoke the current session and clear refresh cookie
       operationId: logout
-      security:
-        - refreshTokenCookie: []
       responses:
         '204':
-          description: Logout successful
-          headers:
-            Set-Cookie:
-              $ref: '#/components/headers/RefreshTokenClearedCookie'
+          description: Session revoked
         '401':
           $ref: '#/components/responses/Unauthorized'
-        '500':
-          $ref: '#/components/responses/InternalError'
   /api/me:
     get:
-      tags: [user]
-      summary: Get the current authenticated user
-      operationId: getCurrentUser
+      tags: [auth]
+      operationId: me
       security:
         - bearerAuth: []
       responses:
         '200':
-          description: Current authenticated user
+          description: Current user
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/UserResponse'
-              examples:
-                default:
-                  value:
-                    user:
-                      id: 8f0ed065-aefa-4f56-87d0-e2ef2ef43f0d
-                      email: user@example.com
+                type: object
+                required: [user]
+                properties:
+                  user:
+                    $ref: '#/components/schemas/User'
         '401':
           $ref: '#/components/responses/Unauthorized'
-        '500':
-          $ref: '#/components/responses/InternalError'
-  /api/pet:
+
+  /api/v1/pet:
     get:
-      tags: [pet]
-      summary: Get or create the authenticated user's Avitosha
+      tags: [game]
       operationId: getPet
       security:
         - bearerAuth: []
+        - userIdHeader: []
       responses:
         '200':
-          description: Current pet and today's UTC state
+          description: Avitosha profile, created on first access
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/PetSnapshotResponse'
+                $ref: '#/components/schemas/PetProfile'
         '401':
           $ref: '#/components/responses/Unauthorized'
-        '500':
-          $ref: '#/components/responses/InternalError'
-  /api/pet/items/{item_id}/use:
-    post:
-      tags: [pet]
-      summary: Consume an inventory item and update Avitosha atomically
-      operationId: usePetItem
+  /api/v1/tasks:
+    get:
+      tags: [game]
+      operationId: listTasks
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      responses:
+        '200':
+          description: Assigned tasks
+          content:
+            application/json:
+              schema:
+                type: object
+                required: [tasks]
+                properties:
+                  tasks:
+                    type: array
+                    items:
+                      $ref: '#/components/schemas/TaskProgress'
+  /api/v1/tasks/{task_id}:
+    get:
+      tags: [game]
+      operationId: getTask
       security:
         - bearerAuth: []
+        - userIdHeader: []
       parameters:
-        - name: item_id
+        - name: task_id
           in: path
           required: true
           schema:
@@ -193,444 +192,537 @@ paths:
             format: uuid
       responses:
         '200':
-          description: Updated pet, daily state, and emitted domain events
+          description: Task progress
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/PetCareResponse'
-        '400':
-          $ref: '#/components/responses/InvalidRequest'
-        '401':
-          $ref: '#/components/responses/Unauthorized'
+                $ref: '#/components/schemas/TaskProgress'
         '404':
-          description: Pet or inventory item was not found
+          $ref: '#/components/responses/NotFound'
+  /api/v1/actions:
+    post:
+      tags: [game]
+      operationId: processAction
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      requestBody:
+        required: true
+        content:
+          application/json:
+            schema:
+              $ref: '#/components/schemas/ActionRequest'
+      responses:
+        '200':
+          description: Atomic action result; duplicate is true for an idempotent retry
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/ErrorResponse'
+                $ref: '#/components/schemas/ActionResult'
+        '400':
+          $ref: '#/components/responses/InvalidRequest'
         '409':
-          description: Inventory item is already used, expired, or otherwise unavailable
+          $ref: '#/components/responses/Conflict'
+  /api/v1/room:
+    get:
+      tags: [game]
+      operationId: getRoom
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      responses:
+        '200':
+          description: Room catalog with LOCKED/PLACED state
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/ErrorResponse'
-        '500':
-          $ref: '#/components/responses/InternalError'
-  /api/pet/daily-summary:
+                $ref: '#/components/schemas/RoomResponse'
+  /api/v1/story:
     get:
-      tags: [pet]
-      summary: Get the previous completed UTC day's pet summary
-      operationId: getPetDailySummary
+      tags: [game]
+      operationId: getStory
       security:
         - bearerAuth: []
+        - userIdHeader: []
       responses:
         '200':
-          description: Previous UTC day summary
+          description: FIRST_ROOM progress
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/PetDailySummaryResponse'
-        '401':
-          $ref: '#/components/responses/Unauthorized'
-        '404':
-          description: No pet daily state exists for the previous UTC day
+                $ref: '#/components/schemas/StoryResponse'
+  /api/v1/daily-summary:
+    get:
+      tags: [game]
+      operationId: getDailySummary
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      responses:
+        '200':
+          description: Current UTC-day summary
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/ErrorResponse'
-        '500':
-          $ref: '#/components/responses/InternalError'
+                $ref: '#/components/schemas/DailySummary'
+  /api/v1/leaderboard:
+    get:
+      tags: [game]
+      operationId: getLeaderboard
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      parameters:
+        - name: period
+          in: query
+          schema:
+            type: string
+            enum: [weekly]
+            default: weekly
+        - name: limit
+          in: query
+          schema:
+            type: integer
+            minimum: 1
+            maximum: 100
+            default: 10
+      responses:
+        '200':
+          description: Weekly top and current user position
+          content:
+            application/json:
+              schema:
+                $ref: '#/components/schemas/LeaderboardResponse'
+        '400':
+          $ref: '#/components/responses/InvalidRequest'
+  /api/v1/achievements:
+    get:
+      tags: [game]
+      operationId: getAchievements
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      responses:
+        '200':
+          description: Achievement catalog and unlock state
+          content:
+            application/json:
+              schema:
+                type: object
+                required: [achievements]
+                properties:
+                  achievements:
+                    type: array
+                    items:
+                      $ref: '#/components/schemas/Achievement'
+  /api/v1/ws:
+    get:
+      tags: [realtime]
+      operationId: gameWebSocket
+      description: WebSocket stream scoped to the authenticated user. Browser clients may pass access_token in the query string.
+      security:
+        - bearerAuth: []
+        - userIdHeader: []
+      parameters:
+        - name: access_token
+          in: query
+          required: false
+          schema:
+            type: string
+      responses:
+        '101':
+          description: Switching Protocols
+        '401':
+          $ref: '#/components/responses/Unauthorized'
+
 components:
   securitySchemes:
     bearerAuth:
       type: http
       scheme: bearer
       bearerFormat: JWT
-      description: Access token sent in the Authorization header.
-    refreshTokenCookie:
+    userIdHeader:
       type: apiKey
-      in: cookie
-      name: refresh_token
-      description: HttpOnly refresh token cookie scoped to /api/auth.
-  headers:
-    RefreshTokenSetCookie:
-      description: |
-        HttpOnly refresh token cookie. In production it must also include Secure.
-        Path is /api/auth and SameSite is Lax.
-      schema:
-        type: string
-        example: refresh_token=<OPAQUE_REFRESH_TOKEN>; Path=/api/auth; HttpOnly; SameSite=Lax; Max-Age=2592000
-    RefreshTokenClearedCookie:
-      description: Clears the refresh token cookie for the current client.
-      schema:
-        type: string
-        example: refresh_token=; Path=/api/auth; HttpOnly; SameSite=Lax; Max-Age=0
+      in: header
+      name: X-User-ID
+      description: Temporary mock integration boundary; the UUID must belong to an existing user.
+
   responses:
     InvalidRequest:
-      description: Malformed or semantically invalid request
-      content:
-        application/json:
-          schema:
-            $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            invalid_email:
-              value:
-                error:
-                  code: invalid_request
-                  message: Email must be a valid email address
-    InvalidCredentials:
-      description: Invalid email or password
-      content:
-        application/json:
-          schema:
-            $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            invalid_credentials:
-              value:
-                error:
-                  code: invalid_credentials
-                  message: Invalid email or password
-    EmailAlreadyExists:
-      description: Email is already registered
+      description: Invalid request
       content:
         application/json:
           schema:
             $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            duplicate_email:
-              value:
-                error:
-                  code: email_already_exists
-                  message: Email already exists
     Unauthorized:
-      description: Missing or invalid authentication
+      description: Authentication required
       content:
         application/json:
           schema:
             $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            unauthorized:
-              value:
-                error:
-                  code: unauthorized
-                  message: Authentication is required
-    RefreshUnauthorized:
-      description: Missing refresh cookie or invalid, expired, revoked refresh session
+    Conflict:
+      description: Resource conflict
       content:
         application/json:
           schema:
             $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            missing_refresh_cookie:
-              value:
-                error:
-                  code: unauthorized
-                  message: Authentication is required
-            expired_session:
-              value:
-                error:
-                  code: session_expired
-                  message: Session has expired or is no longer valid
-    SessionExpired:
-      description: Refresh session is expired, revoked, or invalid
+    NotFound:
+      description: Resource not found
       content:
         application/json:
           schema:
             $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            expired_session:
-              value:
-                error:
-                  code: session_expired
-                  message: Session has expired or is no longer valid
     InternalError:
-      description: Unexpected internal server error
+      description: Internal server error
       content:
         application/json:
           schema:
             $ref: '#/components/schemas/ErrorResponse'
-          examples:
-            internal_error:
-              value:
-                error:
-                  code: internal_error
-                  message: Internal server error
+
   schemas:
-    AuthCredentialsRequest:
+    ErrorResponse:
+      type: object
+      required: [error]
+      properties:
+        error:
+          type: object
+          required: [code, message]
+          properties:
+            code:
+              type: string
+            message:
+              type: string
+    AuthCredentials:
       type: object
       additionalProperties: false
-      required:
-        - email
-        - password
+      required: [email, password]
       properties:
         email:
           type: string
           format: email
-          minLength: 3
-          maxLength: 320
-          example: user@example.com
         password:
           type: string
-          format: password
           minLength: 8
           maxLength: 128
-          example: password123
-      description: |
-        Registration and login credentials.
-        The auth contract intentionally does not include display_name.
     User:
       type: object
-      additionalProperties: false
-      required:
-        - id
-        - email
+      required: [id, email]
       properties:
         id:
           type: string
           format: uuid
-          example: 8f0ed065-aefa-4f56-87d0-e2ef2ef43f0d
         email:
           type: string
           format: email
-          example: user@example.com
     AuthResponse:
       type: object
-      additionalProperties: false
-      required:
-        - access_token
-        - user
+      required: [access_token, user]
       properties:
         access_token:
           type: string
-          description: JWT access token returned in JSON response body.
-          example: <JWT_ACCESS_TOKEN>
         user:
           $ref: '#/components/schemas/User'
-    RefreshResponse:
+    PetMood:
+      type: string
+      enum: [CALM, CURIOUS, HAPPY, EXCITED, PROUD, SLEEPING]
+    PetCharacter:
+      type: string
+      enum: [EXPLORER, ENTREPRENEUR, MECHANIC, TRAVELER, ARCHITECT, CRAFTSPERSON]
+    CharacterProfile:
       type: object
-      additionalProperties: false
-      required:
-        - access_token
+      required: [code, name, description, iconKey, visualDetail, progress, target, unlocked]
       properties:
-        access_token:
+        code:
+          $ref: '#/components/schemas/PetCharacter'
+        name:
+          type: string
+        description:
+          type: string
+        iconKey:
+          type: string
+        visualDetail:
           type: string
-          description: Newly issued JWT access token.
-          example: <JWT_ACCESS_TOKEN>
-    UserResponse:
+        progress:
+          type: integer
+          minimum: 0
+        target:
+          type: integer
+          minimum: 1
+        unlocked:
+          type: boolean
+    CurrentStory:
       type: object
-      additionalProperties: false
-      required:
-        - user
+      required: [code, title, currentStage, totalStages, status]
       properties:
-        user:
-          $ref: '#/components/schemas/User'
-    Pet:
+        code:
+          type: string
+        title:
+          type: string
+        currentStage:
+          type: integer
+        totalStages:
+          type: integer
+        status:
+          type: string
+          enum: [ACTIVE, COMPLETED]
+    PetProfile:
       type: object
-      additionalProperties: false
-      required: [id, name, level, growth_xp, created_at, updated_at]
+      required: [id, name, level, growthXp, mood, characterProfile, currentStory]
       properties:
         id:
           type: string
           format: uuid
         name:
           type: string
-          example: Авитоша
         level:
           type: integer
           minimum: 1
           maximum: 5
-        growth_xp:
+        growthXp:
           type: integer
           minimum: 0
-        created_at:
-          type: string
-          format: date-time
-        updated_at:
-          type: string
-          format: date-time
-    PetDailyState:
+        nextLevelXp:
+          type: integer
+          nullable: true
+        mood:
+          $ref: '#/components/schemas/PetMood'
+        character:
+          allOf:
+            - $ref: '#/components/schemas/PetCharacter'
+          nullable: true
+        characterProfile:
+          $ref: '#/components/schemas/CharacterProfile'
+        currentStory:
+          $ref: '#/components/schemas/CurrentStory'
+    ActionType:
+      type: string
+      enum: [AD_VIEWED, AD_FAVORITED, MESSAGE_SENT, AD_CREATED, DELIVERY_USED, REVIEW_LEFT, BOOKING_CREATED]
+    TaskProgress:
       type: object
-      additionalProperties: false
-      required: [id, date, satiety, mood, curiosity, state, happy_xp_granted, ecstatic_xp_granted, created_at, updated_at]
+      required: [id, code, title, description, petPhrase, actionType, progress, target, status, xpReward, avitoRewardAmount]
       properties:
         id:
           type: string
           format: uuid
-        date:
+        code:
           type: string
-          format: date
-        satiety:
+        title:
+          type: string
+        description:
+          type: string
+        petPhrase:
+          type: string
+        actionType:
+          $ref: '#/components/schemas/ActionType'
+        category:
+          type: string
+          nullable: true
+        progress:
           type: integer
-          minimum: 0
-          maximum: 100
-        mood:
+        target:
           type: integer
-          minimum: 0
-          maximum: 100
-        curiosity:
+        status:
+          type: string
+          enum: [ACTIVE, COMPLETED, REWARDED, EXPIRED]
+        xpReward:
           type: integer
-          minimum: 0
-          maximum: 100
-        state:
+        roomItemCode:
           type: string
-          enum: [CURIOUS, HUNGRY, BORED, CONTENT, HAPPY, ECSTATIC]
-        happy_xp_granted:
-          type: boolean
-        ecstatic_xp_granted:
-          type: boolean
-        created_at:
+          nullable: true
+        avitoRewardType:
           type: string
-          format: date-time
-        updated_at:
+          nullable: true
+        avitoRewardAmount:
+          type: integer
+        storyStage:
+          type: integer
+          nullable: true
+    ActionRequest:
+      type: object
+      additionalProperties: false
+      required: [eventId, type, occurredAt, metadata]
+      properties:
+        eventId:
+          type: string
+          format: uuid
+        type:
+          $ref: '#/components/schemas/ActionType'
+        entityId:
+          type: string
+        category:
+          type: string
+        occurredAt:
           type: string
           format: date-time
-    PetSnapshot:
+        metadata:
+          type: object
+          additionalProperties: true
+    DomainEvent:
       type: object
-      additionalProperties: false
-      required: [pet, daily_state]
+      required: [id, type, occurredAt]
+      additionalProperties: true
       properties:
-        pet:
-          $ref: '#/components/schemas/Pet'
-        daily_state:
-          $ref: '#/components/schemas/PetDailyState'
-    PetSnapshotResponse:
+        id:
+          type: string
+          format: uuid
+        type:
+          type: string
+        occurredAt:
+          type: string
+          format: date-time
+    ActionResult:
       type: object
-      additionalProperties: false
-      required: [data]
+      required: [actionId, duplicate, events]
       properties:
-        data:
-          $ref: '#/components/schemas/PetSnapshot'
-    PetEvent:
+        actionId:
+          type: string
+          format: uuid
+        duplicate:
+          type: boolean
+        events:
+          type: array
+          items:
+            $ref: '#/components/schemas/DomainEvent'
+    RoomItem:
       type: object
-      additionalProperties: false
-      required: [type, occurred_at, previous_state, new_state, previous_level, new_level, xp_delta]
+      required: [code, name, description, status, assetKey, positionKey]
       properties:
-        type:
+        code:
           type: string
-          enum: [PET_STATE_CHANGED, PET_BECAME_HAPPY, PET_BECAME_ECSTATIC, PET_LEVEL_UP]
-        occurred_at:
+        name:
           type: string
-          format: date-time
-        previous_state:
+        description:
           type: string
-          enum: [CURIOUS, HUNGRY, BORED, CONTENT, HAPPY, ECSTATIC]
-        new_state:
+        status:
           type: string
-          enum: [CURIOUS, HUNGRY, BORED, CONTENT, HAPPY, ECSTATIC]
-        previous_level:
-          type: integer
-          minimum: 1
-          maximum: 5
-        new_level:
-          type: integer
-          minimum: 1
-          maximum: 5
-        xp_delta:
-          type: integer
-          minimum: 0
-    PetCareResult:
-      allOf:
-        - $ref: '#/components/schemas/PetSnapshot'
-        - type: object
-          required: [events]
-          properties:
-            events:
-              type: array
-              items:
-                $ref: '#/components/schemas/PetEvent'
-    PetCareResponse:
+          enum: [LOCKED, UNLOCKED, PLACED]
+        assetKey:
+          type: string
+        positionKey:
+          type: string
+        unlockTaskCode:
+          type: string
+          nullable: true
+    RoomResponse:
       type: object
-      additionalProperties: false
-      required: [data]
+      required: [storyCode, progress, items]
       properties:
-        data:
-          $ref: '#/components/schemas/PetCareResult'
-    PetNeedChange:
+        storyCode:
+          type: string
+        progress:
+          type: string
+        items:
+          type: array
+          items:
+            $ref: '#/components/schemas/RoomItem'
+    StoryResponse:
       type: object
-      additionalProperties: false
-      required: [type, starting_value, ending_value, delta]
+      required: [code, title, description, currentStage, totalStages, status]
       properties:
-        type:
+        code:
           type: string
-          enum: [SATIETY, MOOD, CURIOSITY]
-        starting_value:
-          type: integer
-        ending_value:
+        title:
+          type: string
+        description:
+          type: string
+        currentStage:
           type: integer
-        delta:
+        totalStages:
           type: integer
-    PetDailySummary:
+        status:
+          type: string
+          enum: [ACTIVE, COMPLETED]
+        nextTask:
+          type: object
+          nullable: true
+          properties:
+            id:
+              type: string
+              format: uuid
+            code:
+              type: string
+            title:
+              type: string
+            roomItemCode:
+              type: string
+              nullable: true
+    DailySummary:
       type: object
-      additionalProperties: false
-      required: [date, pet_id, pet_name, starting_state, ending_state, need_changes, starting_growth_xp, earned_growth_xp, ending_growth_xp, starting_level, ending_level, achievements]
+      required: [date, actionsCount, completedTasks, earnedXp, levelBefore, levelAfter, unlockedRoomItems, storyStageBefore, storyStageAfter, weeklyScoreDelta, petMood]
       properties:
         date:
           type: string
           format: date
-        pet_id:
-          type: string
-          format: uuid
-        pet_name:
-          type: string
-        starting_state:
-          type: string
-          enum: [CURIOUS, HUNGRY, BORED, CONTENT, HAPPY, ECSTATIC]
-        ending_state:
-          type: string
-          enum: [CURIOUS, HUNGRY, BORED, CONTENT, HAPPY, ECSTATIC]
-        need_changes:
-          type: array
-          items:
-            $ref: '#/components/schemas/PetNeedChange'
-        starting_growth_xp:
+        actionsCount:
           type: integer
-        earned_growth_xp:
+        completedTasks:
           type: integer
-        ending_growth_xp:
+        earnedXp:
           type: integer
-        starting_level:
+        levelBefore:
           type: integer
-        ending_level:
+        levelAfter:
           type: integer
-        achievements:
+        unlockedRoomItems:
           type: array
           items:
             type: string
-            enum: [BECAME_HAPPY, BECAME_ECSTATIC, LEVEL_UP]
-    PetDailySummaryResponse:
+        storyStageBefore:
+          type: integer
+        storyStageAfter:
+          type: integer
+        weeklyScoreDelta:
+          type: integer
+        weeklyPosition:
+          type: integer
+          nullable: true
+        petMood:
+          $ref: '#/components/schemas/PetMood'
+    LeaderboardEntry:
       type: object
-      additionalProperties: false
-      required: [data]
+      required: [position, userId, petName, level, score, completedTasks]
       properties:
-        data:
-          $ref: '#/components/schemas/PetDailySummary'
-    ErrorDetail:
+        position:
+          type: integer
+        userId:
+          type: string
+          format: uuid
+        petName:
+          type: string
+        level:
+          type: integer
+        score:
+          type: integer
+        completedTasks:
+          type: integer
+    LeaderboardResponse:
       type: object
-      additionalProperties: false
-      required:
-        - code
-        - message
+      required: [weekStart, leaders, currentUser]
       properties:
-        code:
-          type: string
-          enum:
-            - invalid_request
-            - invalid_credentials
-            - unauthorized
-            - email_already_exists
-            - session_expired
-            - pet_not_found
-            - pet_day_not_found
-            - item_not_found
-            - item_unavailable
-            - internal_error
-        message:
+        weekStart:
           type: string
-    ErrorResponse:
+          format: date
+        leaders:
+          type: array
+          items:
+            $ref: '#/components/schemas/LeaderboardEntry'
+        currentUser:
+          $ref: '#/components/schemas/LeaderboardEntry'
+    Achievement:
       type: object
-      additionalProperties: false
-      required:
-        - error
+      required: [code, title, description, iconKey, unlocked]
       properties:
-        error:
-          $ref: '#/components/schemas/ErrorDetail'
+        code:
+          type: string
+        title:
+          type: string
+        description:
+          type: string
+        iconKey:
+          type: string
+        unlocked:
+          type: boolean
+        unlockedAt:
+          type: string
+          format: date-time
+          nullable: true
diff --git a/app/backend/api/openapi_test.go b/app/backend/api/openapi_test.go
index cae6d86..4d978fe 100644
--- a/app/backend/api/openapi_test.go
+++ b/app/backend/api/openapi_test.go
@@ -55,7 +55,7 @@ func TestRefreshUnauthorizedContract(t *testing.T) {
 	}
 }
 
-func TestPetAPIContract(t *testing.T) {
+func TestGameAPIContract(t *testing.T) {
 	t.Parallel()
 
 	loader := openapi3.NewLoader()
@@ -68,9 +68,16 @@ func TestPetAPIContract(t *testing.T) {
 		path   string
 		method string
 	}{
-		{path: "/api/pet", method: "GET"},
-		{path: "/api/pet/items/{item_id}/use", method: "POST"},
-		{path: "/api/pet/daily-summary", method: "GET"},
+		{path: "/api/v1/pet", method: "GET"},
+		{path: "/api/v1/tasks", method: "GET"},
+		{path: "/api/v1/tasks/{task_id}", method: "GET"},
+		{path: "/api/v1/actions", method: "POST"},
+		{path: "/api/v1/room", method: "GET"},
+		{path: "/api/v1/story", method: "GET"},
+		{path: "/api/v1/daily-summary", method: "GET"},
+		{path: "/api/v1/leaderboard", method: "GET"},
+		{path: "/api/v1/achievements", method: "GET"},
+		{path: "/api/v1/ws", method: "GET"},
 	}
 	for _, tt := range tests {
 		path := doc.Paths.Find(tt.path)
@@ -83,12 +90,15 @@ func TestPetAPIContract(t *testing.T) {
 			t.Errorf("%s %s operation is missing", tt.method, tt.path)
 			continue
 		}
-		if operation.Security == nil || len(*operation.Security) == 0 {
-			t.Errorf("%s %s must require bearer authentication", tt.method, tt.path)
+		if operation.Security == nil || len(*operation.Security) != 2 {
+			t.Errorf("%s %s must support bearer and X-User-ID authentication", tt.method, tt.path)
 		}
 	}
 
-	for _, schema := range []string{"PetSnapshotResponse", "PetCareResponse", "PetDailySummaryResponse"} {
+	for _, schema := range []string{
+		"PetProfile", "TaskProgress", "ActionRequest", "ActionResult", "RoomResponse",
+		"StoryResponse", "DailySummary", "LeaderboardResponse", "Achievement",
+	} {
 		if _, ok := doc.Components.Schemas[schema]; !ok {
 			t.Errorf("schema %s is missing", schema)
 		}
diff --git a/app/backend/scripts/init-test-db.sh b/app/backend/scripts/init-test-db.sh
new file mode 100755
index 0000000..1cd392c
--- /dev/null
+++ b/app/backend/scripts/init-test-db.sh
@@ -0,0 +1,6 @@
+#!/usr/bin/env sh
+set -eu
+
+psql --variable ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-SQL
+  CREATE DATABASE "${POSTGRES_DB}_test";
+SQL
diff --git a/app/backend/scripts/smoke-game.sh b/app/backend/scripts/smoke-game.sh
new file mode 100755
index 0000000..72f9d38
--- /dev/null
+++ b/app/backend/scripts/smoke-game.sh
@@ -0,0 +1,47 @@
+#!/usr/bin/env sh
+set -eu
+
+api_url="${API_URL:-http://localhost:8080}"
+email="smoke-$(date +%s)-$$@example.com"
+password="avitosha-smoke-password"
+
+require_command() {
+  command -v "$1" >/dev/null 2>&1 || {
+    echo "required command is missing: $1" >&2
+    exit 1
+  }
+}
+
+require_command curl
+require_command jq
+require_command uuidgen
+
+register_response="$(curl --fail --silent --show-error \
+  -H 'Content-Type: application/json' \
+  -d "{\"email\":\"$email\",\"password\":\"$password\"}" \
+  "$api_url/api/auth/register")"
+access_token="$(printf '%s' "$register_response" | jq -er '.access_token')"
+auth_header="Authorization: Bearer $access_token"
+
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/pet" | jq -e '.mood == "CALM"' >/dev/null
+task_id="$(curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/tasks" | jq -er '.tasks[0].id')"
+
+index=1
+while [ "$index" -le 5 ]; do
+  event_id="$(uuidgen | tr '[:upper:]' '[:lower:]')"
+  curl --fail --silent --show-error \
+    -H "$auth_header" \
+    -H 'Content-Type: application/json' \
+    -d "{\"eventId\":\"$event_id\",\"type\":\"AD_VIEWED\",\"entityId\":\"smoke-ad-$index\",\"category\":\"FURNITURE\",\"occurredAt\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\",\"metadata\":{\"source\":\"smoke\"}}" \
+    "$api_url/api/v1/actions" | jq -e '.duplicate == false' >/dev/null
+  index=$((index + 1))
+done
+
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/tasks/$task_id" | jq -e '.status == "REWARDED" and .progress == 5' >/dev/null
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/pet" | jq -e '.growthXp == 30 and .mood == "PROUD"' >/dev/null
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/room" | jq -e '.items[] | select(.code == "DESK" and .status == "PLACED")' >/dev/null
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/story" | jq -e '.currentStage == 1' >/dev/null
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/daily-summary" | jq -e '.actionsCount == 5 and .earnedXp == 30' >/dev/null
+curl --fail --silent --show-error -H "$auth_header" "$api_url/api/v1/leaderboard?period=weekly&limit=10" | jq -e '.currentUser.score == 100' >/dev/null
+
+echo "Avitosha smoke scenario passed for $email"
diff --git a/app/backend/scripts/smoke-pet.ps1 b/app/backend/scripts/smoke-pet.ps1
deleted file mode 100644
index 08bdbdd..0000000
--- a/app/backend/scripts/smoke-pet.ps1
+++ /dev/null
@@ -1,173 +0,0 @@
-param(
-    [string]$DatabaseURL = $env:TEST_DATABASE_URL,
-    [string]$PsqlPath = 'C:\Program Files\PostgreSQL\17\bin\psql.exe',
-    [int]$APIPort = 18081
-)
-
-Set-StrictMode -Version Latest
-$ErrorActionPreference = 'Stop'
-
-if ([string]::IsNullOrWhiteSpace($DatabaseURL)) {
-    throw 'DatabaseURL or TEST_DATABASE_URL is required'
-}
-if (-not (Test-Path -LiteralPath $PsqlPath)) {
-    throw "psql was not found at $PsqlPath"
-}
-
-$tempRoot = [IO.Path]::GetFullPath([IO.Path]::GetTempPath())
-$runtime = Join-Path $tempRoot ("avitosha-pet-smoke-" + [guid]::NewGuid().ToString('N'))
-$backendRoot = (Resolve-Path (Join-Path $PSScriptRoot '..')).Path
-$binary = Join-Path $runtime 'avitosha-api.exe'
-$stdout = Join-Path $runtime 'stdout.log'
-$stderr = Join-Path $runtime 'stderr.log'
-$process = $null
-$expectedPetName = [Text.Encoding]::UTF8.GetString(
-    [Convert]::FromBase64String('0JDQstC40YLQvtGI0LA=')
-)
-
-New-Item -ItemType Directory -Path $runtime | Out-Null
-
-try {
-    $env:DATABASE_URL = $DatabaseURL
-    $env:APP_ENV = 'test'
-    $env:HTTP_ADDR = "127.0.0.1:$APIPort"
-    $env:FRONTEND_ORIGIN = 'http://localhost:3000'
-    $env:JWT_SIGNING_KEY = 'integration-smoke-signing-key'
-    $env:JWT_ISSUER = 'avitosha-smoke'
-    $env:JWT_AUDIENCE = 'avitosha-web-smoke'
-    $env:LOG_LEVEL = 'info'
-
-    Push-Location $backendRoot
-    try {
-        go build -o $binary ./cmd/api
-        if ($LASTEXITCODE -ne 0) {
-            throw 'API build failed'
-        }
-    }
-    finally {
-        Pop-Location
-    }
-
-    $process = Start-Process -FilePath $binary `
-        -WorkingDirectory $backendRoot `
-        -WindowStyle Hidden `
-        -RedirectStandardOutput $stdout `
-        -RedirectStandardError $stderr `
-        -PassThru
-
-    $baseURL = "http://127.0.0.1:$APIPort"
-    $ready = $null
-    $deadline = (Get-Date).AddSeconds(20)
-    while ((Get-Date) -lt $deadline) {
-        if ($process.HasExited) {
-            $errorLog = Get-Content -LiteralPath $stderr -Raw -ErrorAction SilentlyContinue
-            throw "API exited before readiness: $errorLog"
-        }
-        try {
-            $ready = Invoke-RestMethod -Method Get -Uri "$baseURL/health/ready" -TimeoutSec 2
-        }
-        catch {
-            $ready = $null
-        }
-        if ($null -ne $ready -and $ready.status -eq 'ok') {
-            break
-        }
-        Start-Sleep -Milliseconds 200
-    }
-    if ($null -eq $ready -or $ready.status -ne 'ok') {
-        throw 'API readiness timeout'
-    }
-
-    $email = "pet-smoke-$([DateTimeOffset]::UtcNow.ToUnixTimeMilliseconds())@example.com"
-    $credentials = @{ email = $email; password = 'password123' } | ConvertTo-Json
-    $registration = Invoke-RestMethod -Method Post `
-        -Uri "$baseURL/api/auth/register" `
-        -ContentType 'application/json' `
-        -Body $credentials
-    $headers = @{ Authorization = "Bearer $($registration.access_token)" }
-    $snapshot = Invoke-RestMethod -Method Get -Uri "$baseURL/api/pet" -Headers $headers
-
-    $userID = $registration.user.id
-    $petID = $snapshot.data.pet.id
-    $itemIDs = @(
-        [guid]::NewGuid().ToString(),
-        [guid]::NewGuid().ToString(),
-        [guid]::NewGuid().ToString()
-    )
-    $sourceIDs = @(
-        [guid]::NewGuid().ToString(),
-        [guid]::NewGuid().ToString(),
-        [guid]::NewGuid().ToString()
-    )
-    $seedItems = @"
-INSERT INTO inventory_items (id, user_id, item_type, status, source_type, source_id, idempotency_key)
-VALUES
-    ('$($itemIDs[0])', '$userID', 'FOOD', 'AVAILABLE', 'SMOKE', '$($sourceIDs[0])', 'smoke-food-$userID'),
-    ('$($itemIDs[1])', '$userID', 'TOY',  'AVAILABLE', 'SMOKE', '$($sourceIDs[1])', 'smoke-toy-$userID'),
-    ('$($itemIDs[2])', '$userID', 'BOOK', 'AVAILABLE', 'SMOKE', '$($sourceIDs[2])', 'smoke-book-$userID');
-"@
-    & $PsqlPath $DatabaseURL -v ON_ERROR_STOP=1 -c $seedItems | Out-Null
-    if ($LASTEXITCODE -ne 0) {
-        throw 'Inventory seed failed'
-    }
-
-    $careResult = $null
-    foreach ($itemID in $itemIDs) {
-        $careResult = Invoke-RestMethod -Method Post `
-            -Uri "$baseURL/api/pet/items/$itemID/use" `
-            -Headers $headers
-    }
-
-    $yesterday = [DateTime]::UtcNow.Date.AddDays(-1).ToString('yyyy-MM-dd')
-    $dailyID = [guid]::NewGuid().ToString()
-    $seedPreviousDay = @"
-INSERT INTO pet_daily_states
-    (id, pet_id, date, satiety, mood, curiosity, state, happy_xp_granted, ecstatic_xp_granted, starting_growth_xp)
-VALUES
-    ('$dailyID', '$petID', '$yesterday', 90, 90, 50, 'HAPPY', TRUE, FALSE, 80);
-"@
-    & $PsqlPath $DatabaseURL -v ON_ERROR_STOP=1 -c $seedPreviousDay | Out-Null
-    if ($LASTEXITCODE -ne 0) {
-        throw 'Previous-day seed failed'
-    }
-    $summary = Invoke-RestMethod -Method Get `
-        -Uri "$baseURL/api/pet/daily-summary" `
-        -Headers $headers
-
-    if ($snapshot.data.pet.name -ne $expectedPetName) {
-        throw "Unexpected pet name: $($snapshot.data.pet.name)"
-    }
-    if ($careResult.data.daily_state.state -ne 'ECSTATIC' -or $careResult.data.pet.growth_xp -ne 40) {
-        throw "Unexpected final care result: $($careResult | ConvertTo-Json -Depth 8 -Compress)"
-    }
-    if ($summary.data.ending_state -ne 'HAPPY' -or $summary.data.earned_growth_xp -ne 30) {
-        throw "Unexpected summary: $($summary | ConvertTo-Json -Depth 8 -Compress)"
-    }
-
-    [pscustomobject]@{
-        Ready              = $ready.status
-        PetName            = $snapshot.data.pet.name
-        FinalState         = $careResult.data.daily_state.state
-        GrowthXP           = $careResult.data.pet.growth_xp
-        ItemsUsed          = $itemIDs.Count
-        SummaryDate        = $summary.data.date
-        SummaryEndingState = $summary.data.ending_state
-        SummaryEarnedXP    = $summary.data.earned_growth_xp
-    }
-}
-finally {
-    if ($null -ne $process -and -not $process.HasExited) {
-        Stop-Process -Id $process.Id -Force
-        if (-not $process.WaitForExit(5000)) {
-            throw "API process $($process.Id) did not exit during cleanup"
-        }
-    }
-    if (Test-Path -LiteralPath $runtime) {
-        $resolvedRuntime = (Resolve-Path -LiteralPath $runtime).Path
-        if (-not $resolvedRuntime.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase) -or
-            -not (Split-Path $resolvedRuntime -Leaf).StartsWith('avitosha-pet-smoke-')) {
-            throw "Refusing to remove unexpected runtime directory: $resolvedRuntime"
-        }
-        Remove-Item -LiteralPath $resolvedRuntime -Recurse -Force
-    }
-}
-- 
2.50.1 (Apple Git-155)

